## 1. 共享输入安全规则

- [x] 1.1 在共享模块中定义输入安全校验结果类型、原因码与统一错误语义
- [x] 1.2 实现聊天输入安全判定器，覆盖空白、长度、控制字符与明显攻击型 payload 检测
- [x] 1.3 为共享判定器补充单元测试，覆盖正常法律问题、边界长度与典型非法载荷

## 2. 前端提交前预检

- [x] 2.1 在聊天输入提交路径接入共享输入安全判定器，命中规则时阻止发送 `/ask`
- [x] 2.2 在聊天界面中区分输入安全拒绝与普通请求失败，展示一致的安全提示文案
- [x] 2.3 为前端状态与组件补充测试，验证本地拦截不会发起后端请求

## 3. 后端权威拒绝

- [x] 3.1 在 `POST /ask` 入口接入服务端输入安全校验，并在命中时提前终止请求
- [x] 3.2 定义并返回结构化输入安全错误响应，确保前端可稳定识别原因码与提示
- [x] 3.3 确认被拒绝请求不会进入检索、模型调用与正常问答日志主链路

## 4. 联调与回归验证

- [x] 4.1 增加后端接口测试，覆盖绕过前端直调 `/ask` 时的安全拒绝行为
- [x] 4.2 增加前后端联调验证，确认本地预检与后端兜底返回同一套错误语义
- [x] 4.3 更新运行说明或相关文档，说明输入安全校验边界、错误语义与测试方式
